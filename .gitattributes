# Set default behaviour, in case users don't have core.autocrlf set.
* text=auto

# Explicititly declare text files we want to always to be normalized and converted
# to native line endings on checkout.
*.c text
*.h text

# Declare files that will always CRLF line endings on checkout.
*.sln text eol=crlf

# Denote all files that are truly binary and should not be modified
*.png binary
*.jpg binary
